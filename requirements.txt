# Reproducing the experiments needs a CUDA GPU (an RTX-class card is plenty;
# the original runs used an RTX 4090). CPU works for the tests and tiny smoke
# runs but not the full sweep.
#
# torch is built against CUDA 12.1. Install via the PyTorch index:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121
# CPU-only alternative:
#   pip install torch==2.5.1+cpu --extra-index-url https://download.pytorch.org/whl/cpu

torch==2.5.1+cu121
numpy==1.26.4
scipy==1.17.1
matplotlib==3.10.8
pytest==9.0.3
tqdm==4.67.3
